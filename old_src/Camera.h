//Camera.h / Camera.cpp
//holds the position and transform data of the world camera
//-mgc480

#include <glm/glm.hpp>    //glm is used for positioning and transforming camera
#include <glm/gtc/matrix_transform.hpp>

class Camera{

public:
	Camera(void);
	~Camera(void);
};